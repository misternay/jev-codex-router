## Summary

<!-- What changed, and what it was before. -->

## Verification

<!-- The commands you ran and what they reported. `npm run check` and `npm test`
     at minimum for anything touching src/. -->

## Changelog

<!-- A user-visible change needs a fragment: add changelog.d/<short-slug>.md
     containing the bullet as it should appear, and do NOT edit CHANGELOG.md.
     Two pull requests editing CHANGELOG.md collide on the same line, and that
     conflict does not resolve on GitHub even though it resolves locally.
     See changelog.d/README.md. Delete this section for an internal-only change. -->
