- **Space Bunny Alpha ships as a checked-in OpenRouter route.** OpenRouter's
  anonymous stealth preview (`openrouter/stealth/space-bunny-alpha`) is free
  while the preview lasts. It reads text and images, and its effort ladder runs
  low through max. Reasoning cannot be turned off. OpenRouter publishes a 1M
  window and a 524,288-token output limit, so the route compacts at 470,000.
  OpenRouter leaves stealth ids out of its public catalog, so this route saves
  running `curate-models` by hand. A route you already curated under the same
  slug gives way to the checked-in one. The undisclosed provider may retain
  prompts and completions.
